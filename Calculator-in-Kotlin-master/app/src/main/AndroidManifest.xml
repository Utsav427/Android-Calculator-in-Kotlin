<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.calculatorkt">

    <application
        android:allowBackup="true"
        android:icon="@drawable/calculator_image"
        android:label="@string/app_name"
        android:roundIcon="@drawable/calculator_image"
        android:supportsRtl="true"
        android:theme="@style/Theme.RishabhCalculator">
        <activity android:name="com.example.calculatorkt.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
